{
  "name": "nae-commerce-parent-directory",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "clean": "cd api && rimraf ./build",
    "build": "cd api && npm run clean && tsc",
    "start": "cd api && nodemon build/app.js",
    "dev:api": "cd api && nodemon src/app.ts"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "crypto-js": "^4.1.1"
  }
}
